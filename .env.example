# URL for your PostgreSQL database (replace with your actual connection string)
DATABASE_URL="postgresql://user:password@host:port/database"

# A strong, unique secret for signing JWTs (replace with a real secret)
JWT_SECRET="YOUR_SUPER_SECRET_KEY_HERE"
